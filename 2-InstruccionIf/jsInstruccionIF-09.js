function mostrar()
{
	let numero;
	let minimo=1
	let maximo=10
	
	/*
	let minimo=1
	let maximo=10
	numero = Math.round(Math.random() * (maximo - minimo) + minimo);
	*/

	numero = Math.round(Math.random() * (maximo - minimo) + minimo);

	alert(numero);

}//FIN DE LA FUNCIÓN